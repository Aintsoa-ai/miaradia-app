diff --git a/node_modules/react-native-android-sms-listener/android/src/main/java/com/centaurwarchief/smslistener/SmsListenerModule.java b/node_modules/react-native-android-sms-listener/android/src/main/java/com/centaurwarchief/smslistener/SmsListenerModule.java
index 621f1b0..c307e00 100644
--- a/node_modules/react-native-android-sms-listener/android/src/main/java/com/centaurwarchief/smslistener/SmsListenerModule.java
+++ b/node_modules/react-native-android-sms-listener/android/src/main/java/com/centaurwarchief/smslistener/SmsListenerModule.java
@@ -2,6 +2,7 @@ package com.centaurwarchief.smslistener;
 
 import android.app.Activity;
 import android.content.BroadcastReceiver;
+import android.content.Context;
 import android.content.IntentFilter;
 import android.os.Build;
 import android.provider.Telephony;
@@ -23,29 +24,21 @@ public class SmsListenerModule extends ReactContextBaseJavaModule implements Lif
     }
 
     private void registerReceiverIfNecessary(BroadcastReceiver receiver) {
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.KITKAT && getCurrentActivity() != null) {
-            getCurrentActivity().registerReceiver(
-                receiver,
-                new IntentFilter(Telephony.Sms.Intents.SMS_RECEIVED_ACTION)
-            );
-            isReceiverRegistered = true;
-            return;
-        }
+        if (isReceiverRegistered) return;
+        
+        IntentFilter filter = new IntentFilter("android.provider.Telephony.SMS_RECEIVED");
+        filter.setPriority(999);
         
-        if (getCurrentActivity() != null) {
-            getCurrentActivity().registerReceiver(
-                    receiver,
-                    new IntentFilter("android.provider.Telephony.SMS_RECEIVED")
-            );
-            isReceiverRegistered = true;
+        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
+            getReactApplicationContext().registerReceiver(receiver, filter, Context.RECEIVER_EXPORTED);
+        } else {
+            getReactApplicationContext().registerReceiver(receiver, filter);
         }
+        isReceiverRegistered = true;
     }
 
     private void unregisterReceiver(BroadcastReceiver receiver) {
-        if (isReceiverRegistered && getCurrentActivity() != null) {
-            getCurrentActivity().unregisterReceiver(receiver);
-            isReceiverRegistered = false;
-        }
+        // Keep listening in background
     }
 
     @Override
@@ -55,12 +48,12 @@ public class SmsListenerModule extends ReactContextBaseJavaModule implements Lif
 
     @Override
     public void onHostPause() {
-        unregisterReceiver(mReceiver);
+        // Do not unregister to keep listening in background
     }
 
     @Override
     public void onHostDestroy() {
-        unregisterReceiver(mReceiver);
+        // Do not unregister so it survives if possible
     }
 
     @Override
